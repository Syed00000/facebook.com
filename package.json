{
  "name": "facebook-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\" --names \"🎨CSS,⚙️API\" --prefix-colors \"cyan,yellow\"",
    "start": "cd backend && npm start",
    "install": "cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
